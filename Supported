local IdentifyExec = function(io)
	local CurrentExploit = identifyexecutor()
	for _,v in pairs(io) do
		if (v:sub(0, CurrentExploit:len()) == CurrentExploit) then
			return true
		end
 	end
	return false
end

local Source = "Vk0USRNB"
local Http = game:HttpGetAsync("https://pastebin.com/raw/"..Source, true)
local ListedExec = loadstring(Http)()
local io = {}

io.Identify = function()
      if (IdentifyExec(ListedExec)) then
          warn('Exploit Supported.')
      else
          local T = Instance.new('Message', workspace)
          T.Text = (identifyexecutor().." Is not supported.")
          delay(3, function()
                game.Debris:AddItem(T, 0)
          end)
	  return
     end
end

return io
